<?php
 
 // Normalmente, as funções retornam algo, para isso, usamos a instrução return;
 // o objetivo é armazenar o valor em uma variável e utilizá-lo psoteriormente no código;

 function soma($n1, $n2){
    return $n1 + $n2;
 }

 echo soma(3, 5);

